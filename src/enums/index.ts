export enum RecordType {
  AUDIO = 'audio',
  VIDEO = 'video',
  SCREEN = 'screen',
}

export enum LOGIN_TYPE {
  CODE = 'CODE',
  ACCOUNT = 'ACCOUNT',
}

export enum LENGTH_LIMIT {
  MIN_11 = 11,
  MAX_11 = 11,
  MIN_18 = 18,
  MAX_18 = 18,
  MIN_15 = 15,
  MAX_15 = 15,
}

// 节点基础类型
export enum HAS_FEILD {
  LABEL = 'label',
  VALUE = 'value',
  CHILDREN = 'children',
  LEAF = 'leaf',
}
